#include "libchata.hpp"

namespace libchata_internal {

void process_changes(InternalFile& file) {
    for (size_t i = 0; i < file.data.size(); i++) {
        
    }
}

} // namespace libchata_internal